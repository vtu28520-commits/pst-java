public class AllPairs {
    public static void printOrderedPairs(int[] arr) {
        int n = arr.length;
        
        for (int i = 0; i < n; i++) {
            
            for (int j = 0; j < n; j++) {
                System.out.println("(" + arr[i] + ", " + arr[j] + ")");
            }
        }
    }

    public static void main(String[] args) {
        int[] arr = {1, 2, 3};
        printOrderedPairs(arr);
    }
